export enum Size {
    s = 's',
    m = 'm',
    l = 'l',
    xl = 'xl',
}

export enum Color {
    primary = 'primary',
    muted = 'muted',
    info = 'info',
    success = 'success',
    danger = 'danger',
    warning = 'warning'
}
