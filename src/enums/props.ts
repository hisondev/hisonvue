export enum BoolString {
    true = 'true',
    false = 'false',
}

export enum InputType {
    text = 'text',
    mask = 'mask',
    number = 'number',
    digit = 'digit',
    date = 'date',
    month = 'month',
    email = 'email',
    password = 'password',
}

export enum EditMode {
    editable = 'editable',
    disable = 'disable',
    readonly = 'readonly',
}

export enum DataStatus {
    R = 'R',
    C = 'C',
    U = 'U',
    D = 'D',
}
